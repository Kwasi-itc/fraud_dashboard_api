version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.11
  build:
    commands:
      - sam build
      - sam deploy --no-confirm-changeset --no-fail-on-empty-changeset --stack-name fraud-py-v1-stack --resolve-s3 --capabilities CAPABILITY_IAM